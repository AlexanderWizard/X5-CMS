<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Кэш публичного фронта
    |--------------------------------------------------------------------------
    |
    | Кэширует HTML «статичных» страниц целиком и поиск шаблонов/блоков по slug.
    | Любое изменение контента в админке (страницы, шаблоны, блоки, меню, футер,
    | настройки, языки) сбрасывает кэш автоматически — через bump «поколения»
    | (см. App\Modules\Cms\Support\FrontCache). Отключать стоит лишь для отладки.
    |
    | Страницы с per-request разметкой (CSRF-токен, old()/$errors, session —
    | например форма обратной связи на главной) HTML-ом НЕ кэшируются никогда:
    | для них работает только кэш поиска шаблонов/блоков.
    |
    */
    'front_cache' => env('CMS_FRONT_CACHE', true),

    /*
    | TTL записей фронт-кэша в секундах. Сброс по факту делает bump поколения —
    | TTL лишь подстраховка от накопления «осиротевших» ключей старых поколений.
    */
    'front_cache_ttl' => (int) env('CMS_FRONT_CACHE_TTL', 86400),
];
